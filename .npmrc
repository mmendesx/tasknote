# Flat node_modules so electron-builder can resolve transitive deps
# (pnpm's symlinked virtual store leaves e.g. electron-updater's fs-extra
# out of the packaged asar, crashing the app at boot).
node-linker=hoisted
