# TaskNote

TaskNote is a local-first, single-user task manager that combines a Kanban board, freeform personal notes, and file references — all running on your machine with a SQLite database, no cloud required. Run `pnpm dev` from the repo root to start both the API (port 3001) and the web app (port 5173) in parallel.
